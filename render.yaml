services:
  - type: web
    name: emis-exam-portal
    env: python
    region: frankfurt
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
